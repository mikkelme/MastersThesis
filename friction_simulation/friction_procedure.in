### Script for running a friction simulation 


variable dt index 0.001 
variable T index 300.0
variable relax_time index 5
variable stretch_speed_pct index 0.1 
variable pause_time1 index 5
variable pause_time2 index 5 
variable drag_speed index 2
variable drag_length index 400
variable K index 0
# variable K index 1.87 # ~ 30 N/m
variable root index ..
variable out_ext index test
variable config_data index sheet_nocut #  sheet_nocut.txt and sheet_nocut_info.in must be present in the folder 
variable stretch_max_pct index 2.0
variable drag_dir_x index 0 
variable drag_dir_y index 1 
variable F_N index 6.241 # 10 nN

variable num_stretch_files index 0
variable run_rupture_test index 0

# variable R index 6
# include ${root}/manual_coupling_stretch.in
# include ${root}/manual_coupling_free_drag.in
# include ${root}/manual_coupling_drag.in

include ${root}/stretch.in
# include ${root}/drag.in 

################
# A script can be directly executed with the command on the format 
# (Run from /my_simulation_space)

# mpirun -n 4 lmp_mpi -in ../friction_procedure.in -var num_stretch_files 3 -var dt 0.001 -var T 100.0 -var relax_time 1 -var pause_time1 5 -var pause_time2 5 -var stretch_speed_pct 0.01 -var drag_speed 0.01 -var drag_length 1.0 -var K 0 -var root .. -var out_ext test -var config_data sheet_only -var stretch_max_pct 0.1 -var drag_dir_x 0 -var drag_dir_y 1 -var F_N 6.24150907 -var run_rupture_test 0

